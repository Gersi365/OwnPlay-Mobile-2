name: QA APK for c90fcc0

on:
  push:
    branches:
      - qa-apk-c90fcc0
    paths:
      - '.github/workflows/qa-apk-c90fcc0.yml'

permissions:
  contents: read

concurrency:
  group: qa-apk-c90fcc0
  cancel-in-progress: false

jobs:
  build-qa-apk:
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Checkout exact validated product source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: c90fcc036b8bcb135696cb6367d9bbbb198f1ac3
          persist-credentials: false

      - name: Verify exact product source
        shell: bash
        run: |
          actual_sha="$(git rev-parse HEAD)"
          test "$actual_sha" = "c90fcc036b8bcb135696cb6367d9bbbb198f1ac3"
          echo "QA APK source HEAD: $actual_sha"

      - name: Set up JDK 17
        uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4
        with:
          cmdline-tools-version: '14742923'
          packages: ''
          log-accepted-android-sdk-licenses: false

      - name: Install Android SDK 36
        shell: bash
        run: |
          sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle 9.6.0
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6
        with:
          gradle-version: '9.6.0'
          cache-provider: basic
          validate-wrappers: true

      - name: Build debug QA APK
        shell: bash
        run: |
          gradle :app:assembleDebug --no-build-cache --stacktrace

      - name: Verify APK and write QA metadata
        id: metadata
        shell: bash
        run: |
          set -euo pipefail
          apk="$(find app/build/outputs/apk/debug -type f -name '*.apk' -print -quit)"
          test -n "$apk"
          mkdir -p qa-artifact
          cp "$apk" qa-artifact/OwnPlay-Mobile-2-qa-c90fcc0.apk
          apk_sha="$(sha256sum qa-artifact/OwnPlay-Mobile-2-qa-c90fcc0.apk | awk '{print $1}')"
          apk_size="$(stat -c '%s' qa-artifact/OwnPlay-Mobile-2-qa-c90fcc0.apk)"
          signer_output="$($ANDROID_HOME/build-tools/36.0.0/apksigner verify --print-certs qa-artifact/OwnPlay-Mobile-2-qa-c90fcc0.apk)"
          signer_sha="$(printf '%s\n' "$signer_output" | sed -n 's/^Signer #1 certificate SHA-256 digest: //p' | head -n 1)"
          test -n "$signer_sha"
          {
            echo 'OwnPlay Mobile 2 — QA APK'
            echo 'Source SHA: c90fcc036b8bcb135696cb6367d9bbbb198f1ac3'
            echo 'Build type: debug QA'
            echo "APK SHA-256: $apk_sha"
            echo "APK bytes: $apk_size"
            echo "Signer certificate SHA-256: $signer_sha"
            echo 'Package: app.ownplay.mobile'
            echo 'Version: 0.1.0-dev (1)'
          } > qa-artifact/QA_BUILD_METADATA.txt
          printf '%s\n' "$signer_output" > qa-artifact/APKSIGNER_VERIFY.txt
          cat qa-artifact/QA_BUILD_METADATA.txt

      - name: Upload QA APK artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: OwnPlay-Mobile-2-qa-c90fcc0
          path: qa-artifact/
          if-no-files-found: error
          retention-days: 7
